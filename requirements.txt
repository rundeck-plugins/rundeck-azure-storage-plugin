# Python dependencies for Rundeck Azure Storage Plugin
# Install with: pip install -r requirements.txt

# Azure Storage SDK
azure-storage-blob>=12.30.0

# CVE-2026-21226 - Deserialization of untrusted data in azure-core
azure-core>=1.38.0

# File type detection
python-magic>=0.4.27

# HTTP client for async operations
aiohttp>=3.14.3

# CVE-2025-66471 / CVE-2025-66418
urllib3>=2.6.3

# CVE-2026-69247 - Bleichenbacher oracle in PKCS#7 EnvelopedData decryption
# Transitive via azure-storage-blob; pin until upstream bumps its dependency
cryptography>=50.0.0

# Additional security considerations:
# - Use virtual environments to isolate dependencies
# - Regularly update to latest versions for security patches
# - Monitor for CVEs in these dependencies